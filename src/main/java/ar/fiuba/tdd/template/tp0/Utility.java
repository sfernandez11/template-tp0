package ar.fiuba.tdd.template.tp0;

import static ar.fiuba.tdd.template.tp0.Constants.*;

public class Utility {

    static boolean isBackSlash(char character) {
        return (character == BackSlash);
    }

    static boolean isOpenSet(char character) {
        return character == OpenSet;
    }
}